{
  "name": "worship-suite",
  "version": "0.1.0",
  "private": true,
  "description": "Worship Suite — Pipeline (Tauri), Charter (web), Vocal Booth (web/PWA), all reading from a shared Supabase backbone.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm -r run dev"
  },
  "devDependencies": {
    "supabase": "^1.219.2",
    "typescript": "^5.6.3"
  }
}
